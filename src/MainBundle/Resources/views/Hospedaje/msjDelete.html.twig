<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal">&times;</button>
    <h4 class="blue bigger">Eliminar Hospedaje</h4>
</div>
<div class="modal-body">
    <form class="form-horizontal" id="formDeleteHosp" action="{{ path('hospedajeDelete') }}" method="POST">
        <input type="hidden" name="idHospedaje" value="{{ hospedaje.id }}">
        Seguro que desea eliminar el Hospedaje <strong>{{ hospedaje.titulo }}</strong>?
    </form>
</div>
<div class="modal-footer">
    <button id="aceptarDeleteHosp" class="btn btn-small btn-primary">
        <i class="icon-ok"></i>
        Aceptar
    </button>
    <button class="btn btn-small" data-dismiss="modal">
        <i class="icon-remove"></i>
        Cancelar
    </button>
</div>
<script type="text/javascript">
    $('#aceptarDeleteHosp').on('click', function (event) {
        event.preventDefault();
        $('#formDeleteHosp').submit();
    });
</script>